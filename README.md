# Homebrew tap for FixThis

```bash
brew tap beyondwin/fixthis
brew install fixthis
```

Then run the agent-first setup from an Android app repository:

```bash
fixthis install-agent --project-dir . --target all
```
